<% layout('layouts/boilerplate') %>
    <h1>
        <%= list.title %>
    </h1>

    <p><strong>Description:</strong>
        <%= list.description %>
    </p>
    <p><strong>Price:</strong> ₹<%= list.price %>
    </p>
    <p><strong>Location:</strong>
        <%= list.location %>, <%= list.country %>
    </p>

    <% if (list.image) { %>
        <img src="<%= list.image %>" alt="Image" width="250">
        <% } %>

            <br><br>
            <a href="/listings/<%= list._id %>/edit"><button class="btn btn-primary">Edit</button></a>

            <form action="/listings/<%= list._id %>?_method=DELETE" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-primary btn-danger">Delete</button>
            </form>

